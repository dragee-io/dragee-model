{
  "name": "dragee-asserter-model",
  "module": "index.ts",
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "build": "bun build ./src/dragee.model.ts --target bun --outdir ./dist && bun run build:declaration",
    "build:declaration": "tsc --emitDeclarationOnly --project tsconfig.json"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}